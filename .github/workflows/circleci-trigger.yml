name: Trigger CircleCI Pipeline

on:
  push:
    branches:
      - main

jobs:
  trigger-circleci:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger CircleCI pipeline
        env:
          CIRCLE_TOKEN: ${{ secrets.CIRCLE_TOKEN }}
          PROJECT_SLUG: ${{ secrets.CIRCLE_PROJECT_SLUG }}
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          echo "Triggering CircleCI pipeline..."

          curl -X POST https://circleci.com/api/v2/project/$PROJECT_SLUG/pipeline/run \
          -H "Circle-Token: $CIRCLE_TOKEN" \
          -H "Content-Type: application/json" \
          -d '{
            "definition_id": "d857a35-1295-4f77-8cd1-8cfd83ae563c",
            "config": { "branch": "'"$BRANCH_NAME"'" },
            "checkout": { "branch": "'"$BRANCH_NAME"'" }
          }'